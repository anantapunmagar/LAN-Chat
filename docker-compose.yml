services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: lanchat
      POSTGRES_PASSWORD: lanchat
      POSTGRES_DB: lanchat
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
